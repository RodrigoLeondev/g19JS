

// let newArray = []
// let newArray = [1.2.4]
// let newArray = ['str', str2]
// let newArray = [ [9.8.7], [9,8]]
/*
let newArray = [1,2,3, 'str', [1,2] ]
    console.log(newArray)
    //.lenght --> tamaño de array  --> cantidad de elementos que contiene

   // console.log(newArray[3][1])

    for( i = 0; i < newArray.length ; i++ ){
        //console.log(newArray[i])
    }  
    let arrayToReverse = [1,2,3]
    let arrReversed = []
    for( i = arrayToReverse.length -1 ; i >= 0 ; i++ ){
        arrReversed.push( arrayToReverse[i])
    }
        console.log(arrayToReverse, arrReversed)
        arrReversed.push(3)
        arr
    // Métodos de array 
    // .push() --> agregar nuevo item al array
    // .pop() --> elimina nuevo item al array
*/

// .map()
/*
let mapArr = [1,2,3,3]
let doubleArr = mapArr.map( (cv,index, arr => {
    let transformado = cv * 2
    return transformado
}))
*/

/*
const getFirstLast = ('hola', 'mundo')
let ultimas = getFirstLast.substring(-2)

console.log(getFirstLast)

*/
// Array. coleccion de datos, string, objetos, etc. siempre dentro de []



// Join()  itera cada uno de los array, se tiene que guardar en variables

//let arrJoin = ('h', 'k')
//arrJoin.arrJoin

//let newArrayJoin ? arrJoin.join( 

/*
    const strReverse = (str) => {
        let strReverse = str.split('').reverse().join('')
        return strReverse
    }
*/
/*
const miFuncion = (name, lastNam) =>{
    console.log(name)
}
miFuncion('Hola')

const pagarTarjeta = (cash, cuenta) => {

    pagarTarjeta(cuenta, cash, identificacion)
}*/
 /*
const hacerCafe (agua, cafe, cafetera, taza, azucar) => {

    agregarIngredientes(cafe, agua)
    Encender(cafetera)
    servirCafe(taza)
    endulzarCafe(taza, cafe)
    mezclar(taza)
} */
/*
const bañarse = (boiler, jabon, agua, toalla, sandalias) => {
    if(verificarBoiler(boiler)){
        abrirRegadera(regadera)
        mojarCuerpo(agua)
        enjabonar(jabon)
        enjuagarse(agua)
        cerrarRegadera(regadera)
        SVGFESpecularLightingElement(toalla)

    }
}*/
 /*
const hacerSalsa = (cuchillo, cebolla, licuadora, jitomate, chiles, sal, ajo, ahua = false) =>{
    cortarIngredientes(cuchillo, jitomate, cebolla)
        if(agua == true){
            colocarLicuadora(licuadora, jitomate, cebolla, chiles, sal, ajo, agua)
        }
        else {
            colocarLicuadora(licuadora, jitomate, cebolla, chiles, sal, ajo)
        }
        licuadora(licuadora)
}*/


// [2,3,4]
// Obtener los pares
/*
const obtenerPares = (arrayDeNumeros ) => {

    
    recorrer cada numero del arrayDeNumeros
    en cada recorrido: 
    --> verificar si es par
    --> si es par : imrprime
    --> si no : seguir
    Terminar recorrido
    */
/*
    recorrerArrayDeNumeros( numero) {
        verificarSiEsPar(numero)
        if(verificarSiEsPar(numero)){
            imprimirEnConsola(numero)
        }
    } */
/*
    arrayDeNumeros.forEach((numero, index, array) => {
        if( numero % 2 === 0 ){
            console.log(`El numero ${numero} es par`)
        }
    })

} 
*/